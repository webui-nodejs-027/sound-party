const confirmTemplate = require('./confirmTemplate');
const newPasswordTemplate = require('./newPasswordTemplate');
const resetPasswordTemplate = require('./resetPasswordTemplate');

module.exports = {
  confirmTemplate,
  newPasswordTemplate,
  resetPasswordTemplate,
};
